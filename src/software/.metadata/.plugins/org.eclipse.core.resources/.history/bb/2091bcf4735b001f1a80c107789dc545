#include <stdio.h>
#include "system.h"
#include <alt_types.h>
#include "lib_KEY/KEY.h"

int main() {
	open_KEY();
    int valor;

    printf("init \n");

    while (1) {
        if (read_KEY(&valor)) {
            printf("Botão foi pressionado %u \n", valor);
        }
    }

    return 0;
}
